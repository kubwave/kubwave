{{- if .Values.worker.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.worker.serviceAccount.name }}
  labels:
    {{- include "kubwave.labels" (dict "root" $ "component" "worker") | nindent 4 }}
{{- end }}
