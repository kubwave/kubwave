apiVersion: v2
name: kubwave
description: kubwave platform — Backend API + Console (Nuxt) + Backend Worker + Postgres (+ Adminer in dev)
type: application
# Dev placeholder — the release pipeline stamps the real version via `helm package --version/--app-version`.
version: 0.0.0-dev
appVersion: '0.0.0-dev'
kubeVersion: '>=1.27.0-0'
