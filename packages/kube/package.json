{
	"name": "@kubwave/kube",
	"version": "0.0.0",
	"description": "kubwave Kubernetes client and workload helpers.",
	"private": true,
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/kubwave/kubwave.git",
		"directory": "packages/kube"
	},
	"bugs": {
		"url": "https://github.com/kubwave/kubwave/issues"
	},
	"homepage": "https://github.com/kubwave/kubwave/tree/main/packages/kube",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "bun test --isolate test",
		"test:coverage": "bun test --isolate --coverage --coverage-reporter=text --coverage-reporter=lcov test",
		"check-types": "tsc --noEmit"
	},
	"dependencies": {
		"@kubernetes/client-node": "^1.0.0"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^6.0.3"
	}
}
